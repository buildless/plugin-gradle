
plugins {
  // 1. load the latest version of the plugin
  id("build.less") version "1.0.0-beta1"
}

buildless {
  // 2. nothing needed if the API key is available via env, properties, or config file
}
